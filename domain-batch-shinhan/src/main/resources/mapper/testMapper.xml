<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paycoms.cp7.batch.shinhan.mapper.TestMapper">
  <select id="findAll" resultType="com.paycoms.cp7.batch.shinhan.domain.TestEntity">
    SELECT id, name, status
      FROM sample_table
     WHERE status = 'READY'
  </select>

  <update id="updateStatus">
    UPDATE sample_table
       SET status = 'DONE'
     WHERE id     = #{id}
  </update>
</mapper>